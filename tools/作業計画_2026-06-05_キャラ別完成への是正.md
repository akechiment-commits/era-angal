# 作業計画 2026-06-05：独自コマンド地の文の「先走り」を是正し、キャラ別「完成」へ戻す

> **履歴資料（完了済み）**：2026-06-05時点の是正計画です。現在の進捗判断には使用せず、`ROADMAP.md` と `TECHNICAL_PLAN.md` を参照してください。

## 0. いま何をしていたか（現状認識）
- 直近タスクは「独自コマンド（ウフフ COM280-284／純愛 COM410-414）の**地の文枠**」を埋めること。
- だが私は **地の文だけを 30 体以上に横断的・順不同で** 投入した（＝先走り）。
- 本来の作業単位は **「1 体を完全に仕上げてから次へ」**。地の文は「完成」の一要素にすぎない。
- 加えて、単発型テンプレ（5 場面が未確定だったキャラ）の場面を **原作を精読せず intro 一行で作っていた**（要是正）。
  - 原作精査は必ず `tools/output/<名前>.txt`（全セリフ）＋ `tools/dialogue_<名前>/`（テーマ別）＋
    `tools/output/<名前>_転校生.txt`（転校生＝恋愛文脈）を開く。`prompt_*.txt`/`設定.txt`はサマリ。

## 1. 「1 体の完成」とは（＝口上作成手順書.md §10 完成チェックリスト）
- **A 前提ゲート**：`設定.txt` に「ステップ0 証拠ブロック」（原作読了の物証＝末尾セリフ転記＋行番号付 verbatim 5 本以上）。明智さん確認。
- **B 網羅性**：空「」**0**。ベース約100＋COM約936 全スロ記入。COM301 は RAND:10 全埋。会話失敗分岐 `IF TFLAG:18 == -1`。
- **C 原作 verbatim**：会話(COM301)・スキンシップ(COM303)・まったり(COM304)に原作セリフを一字一句 各1〜2本（全キャラ必須）。
- **D 名前呼び・呼称**：`%CALLNAME:PLAYER%`（素の%CALLNAME%は0）。女性同士COMの「この子/あの子/その子」**0**→`%LOCALS%`＋`CALL AITE_YOBI`アンカー。
- **E 性愛系**：視点注釈 `;▼【視点】` に従う／プレイヤーは常に男性・性別変更不可／「説明」でなく「実演」。
- **F 山場・絶頂勾配**：プロポーズ/結婚EN/卒業EN/告白/初回妊娠出産は長文化。絶頂は崩壊度勾配。
- **G 文体・エンジン安全**：cp932+CRLF。半角チルダ `~`(U+007E) **0**。RAND群「ん…」始まりは1群1つ。
- **＋（新）独自コマンド地の文**：280-284/410-414 の地の文枠（先頭場面は原作・既存ブロックAと整合）。
- 監査：`python3 tools/audit_koujo_drift.py [cno]` で 空/この子/LOCALS/AITE/~/CN! を数値確認。

## 2. 現状の進捗（実測 2026-06-05）
- **実装済み（全スロ記入）**：72 あんず以外の 71 体。
- **完成パス済（整合記録あり）**：02-22 + 42 のみ。→ 23-41, 43-72 は**未完成**。
- **地の文だけ投入済（順不同・先走り）**：01-10, 13, 15, 17, 20, 22, 25-29, 31, 34, 36, 38, 42, 46, 47, 49, 52, 55, 56, 59, 60, 61, 72。
  - うち 22/49/72 は場面確定済み型、それ以外の単発型は intro ベースで場面を発想（原作再精査の対象）。
  - 61 のみ原作精読ベースで地の文を作成済み（見本）。
- 監査ドリフト上位＝未完成の指標（72=54, 46=42, 65=42, 60=40, 34=39 …）。

## 3. これからやるべきこと（是正方針）
1. **まず 61（鶴海ひまり）を完全に仕上げる**（着手済みゆえ最後まで）。61 の不足（監査値・ドリフト33）：
   - 空「」**22**（COM69×2 / 160×2 / 188×2 / 189×2 / 300×2 / 323×12）
   - 「この子」**11** → `%LOCALS%`（相手呼称）
   - 原作 verbatim（COM301/303/304）の仕込み／視点・声(一人称「私」)・山場の点検
   - 地の文（投入済）
   - `設定.txt`（ステップ0 証拠）＋`整合記録` 作成 → 監査で 空0・この子0 を確認
2. 以後は **1 体ずつ §10 で完成**させる。**順不同の横断作業はしない**。
3. 着手順は要相談（案：①01 から順／②監査ドリフト降順／③地の文投入済みの体から優先）。
4. 既に「地の文だけ」入れた順不同分は完成ではない。各体を完成パスで仕上げる際に、
   単発型の場面も原作精読で再点検し統合する。

## 4. 厳守（CLAUDE.md / 手順書）
- ERB/CSV は **Python(cp932+CRLF)** のみ。**Edit ツール禁止**。
- master は触らない。作業ブランチへ随時コミット可。
- 着手・完成・監査の前に §10 完成チェックリストを上から照合する。
