# パートナーターン設計・保守メモ

更新日：2026-07-31  
状態：実装完了。今後は回帰確認とバランス調整のみ。

## 役割

通常コマンド終了後、パートナーが状態に応じて一手だけ自発行動する。
手動の完全主導権モード、連続自動調教、耐力消費オプションとは別機能。

## 変更時の入口

| ファイル | 役割 |
|---|---|
| `tools/add_auto_training_mode.py` | パートナーターン本体の生成元 |
| `ERB/PARTNER_TURN_パートナーターン.ERB` | 候補抽出・重み付け・実行 |
| `ERB/PASSIVE_パッシブスキル.ERB` | パッシブ行動名と通常COMへの変換 |
| `ERB/COMF/COMABLE.ERB` | 通常COMの実行可否 |
| `ERB/REVMODE_リバースモード.ERB` | パートナーターン中の消費処理 |
| `ERB/COUNTERSYSTEM_カウンターシステム.ERB` | 射精時の耐力ダメージ |
| `ERB/SYSTEM_基本システム処理.ERB` | 体力0時の終了処理 |
| `ERB/SELF_セルフコマンド.ERB` | 調教後行為 |

ERB/CSVはCP932・CRLF。生成スクリプトを編集し、2回実行して冪等性を確認する。

## 現在の候補ルール

- 関係が浅い、合意前、着衣中などでは日常交流を中心にする。
- 明確な性行為は、ウフフ中・脱衣済み・恋慕・合意に加え、
  「恋人または淫乱」を満たす場合だけ候補へ入れる。
- 候補はパッシブ行動から通常COMへ変換した後、対応する `COM_ABLE` を通す。
- 判定時だけ `TFLAG:53 = 0`、`TFLAG:DOTRAIN = 1` とし、判定後に必ず復元する。
- ウフフ中に候補が空でも会話へ強制フォールバックしない。
  非ウフフ中だけ談笑を最終フォールバックにする。
- 押し倒し／脱衣演出は相手ターン候補にせず、ウフフ移行処理へ一本化する。

## 重要な変換

| パッシブ行動 | 通常COM | 注意 |
|---|---:|---|
| バード／ソフト／ディープキス | 非ウフフ時340～342、ウフフ中6 | COM120～124へ変換しない |
| 性器擦り合い | 主人公に男性器あり33、それ以外63 | 性別構成で分岐 |
| クンニ | 1 | ウフフ中の正常な候補 |
| 騎乗位 | 34 | 基礎重み8＋欲情補正、旧重み500は廃止 |
| 玉揉み | 対応COMを維持 | 強度問題を理由に候補から削除しない |

## 重み付け上の注意

- キス3種類はウフフ中にすべてCOM6へ合流するため、重みが三重加算される。
  各キスの親密補正は低めかつ最大6に制限している。
- 性的候補へ「恋人かつ淫乱」を要求すると、恋人だが淫乱でない相手が
  キス以外を選べなくなる。「恋人または淫乱」を維持する。
- 騎乗位だけ大きな固定値を与えると候補が偏る。能力値勾配を他行動と揃える。

## 消費・終了処理

- パートナーターン中は `TFLAG:543` を使用し、体力・気力・耐力消費を半減する。
- 射精時の耐力ダメージも同フラグ中は半減する。
- パートナー体力による強制終了は `BASE:0 <= 0` の時だけ。
- 体力1～499では調教を継続でき、任意終了時の調教後行為も許可する。
- 体力0終了時だけ `TFLAG:301 = 1` で調教後行為を、
  `TFLAG:999 = 1` で終了後の追加パートナーターンを抑止する。
- 体力0時に旧反発感情マークは付与しない。

## 射精量・耐力

- 射精ゲージの安全上限は `1000000000000000`。負値は0へ戻す。
- 耐力ダメージの連射倍率は `SQRT(TFLAG:714)`。
- 基礎係数600、絶頂強度1段階につき2%、追加多重絶頂10%。
- 内部倍率は画面へ表示せず、耐力ダメージと絶頂強度だけを表示する。
- 主人公耐力成長は1回10点＋実射精量1mlにつき1点。

## 旧セーブ互換

- 独自コマンド名 `CSTR:80-89` はロード時にCSV定義へ同期する。
- 廃止・変更した候補番号を再利用する時は、旧セーブ値との衝突を先に確認する。
- 新しいFLAG領域は `tools/audit_flag_collisions.py` と
  `ERB/FLAG_MIGRATION_フラグ領域移行.ERB` を確認する。

## 回帰確認

変更後は最低限、次を確認する。

1. 非ウフフ時に談笑・キス・抱擁などが混ざる。
2. ウフフ中に会話や非ウフフ専用コマンドが出ない。
3. 恋人だが淫乱でない相手でも、実行可能な性的候補がキス以外に入る。
4. 騎乗位だけに偏らず、手淫・口淫・性器擦り合い等が混ざる。
5. 男性主人公の性器擦り合いで快感・射精ゲージが加算される。
6. パートナー体力1では継続し、0でのみ事後行為と追加ターンを飛ばす。
7. 通常行動とパートナーターンで耐力ダメージが不自然に乖離しない。

## 過去に起きた回帰

- `TFLAG:53`を立てたまま `COM_ABLE` を呼び、候補が全落ちした。
- 候補なしを会話へフォールバックし、ウフフ中も会話固定になった。
- 性的候補へ恋人と淫乱の両方を要求し、キス固定になった。
- 騎乗位へ固定重み500を与え、騎乗位固定になった。
- 通常キスをキス性交へ誤変換した。

候補を変更する際は、必ず
「パッシブ行動 → 変換先COM → COM_ABLE → SOURCE／経験値」
の経路を一続きで確認する。
